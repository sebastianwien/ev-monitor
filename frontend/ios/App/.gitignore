# Build-Output
build/
DerivedData/
# Signing-Artefakte NIEMALS committen (oeffentliches Repo)
*.mobileprovision
*.cer
*.certSigningRequest
*.p12

# Store-Screenshots (Binaries, regenerierbar, echte Prod-Daten) + deliver-Artefakte
fastlane/screenshots/
fastlane/preview.html
*.itmsp
