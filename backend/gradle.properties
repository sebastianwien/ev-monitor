# Build-Cache: unveraenderte compileJava/compileTestJava-Outputs werden
# wiederverwendet statt neu uebersetzt. Auf CI greift das, weil ~/.gradle/caches
# ueber setup-java (cache: 'gradle') zwischen Runs erhalten bleibt.
org.gradle.caching=true
org.gradle.parallel=true
